"use client"
export default function CareerFilters() { return <div className="text-sm text-muted-foreground">Filters are applied via URL params (search, employmentType, location, remote, industry, company, salaryMin, salaryMax).</div> }
